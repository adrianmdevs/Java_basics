public class ShortHandIfElse {
    public static void main(String[] args) {
        int setIntervalPhase = 80;
        String expectedPhaseOut = (setIntervalPhase > 50) ? "The Timeout is Good" : "The Timeout is too low";
        System.out.println(expectedPhaseOut);
    }
    

}
